---
# ============================================================
# MASTER ENTRY POINT
#
# Run all sections:
#   ansible-playbook main.yml
#
# Run specific sections by tag:
#   ansible-playbook main.yml --tags bgp,ospf
#
# Target a single device:
#   ansible-playbook main.yml --limit router1
#
# Use the lab inventory instead of production:
#   ansible-playbook main.yml -i inventory/lab/hosts.yml
#
# Dry run (check mode):
#   ansible-playbook main.yml --check
# ============================================================
- name: Cisco IOS-XE Troubleshooting
  hosts: ios_xe
  gather_facts: no
  vars_files:
    - inventory/production/group_vars/ios_xe.yml
  roles:
    - ios_xe_troubleshoot

- name: Cisco NX-OS Troubleshooting
  hosts: nxos
  gather_facts: no
  vars_files:
    - inventory/production/group_vars/nxos.yml
  roles:
    - nxos_troubleshoot

- name: Cisco ASA Troubleshooting
  hosts: asa
  gather_facts: no
  vars_files:
    - inventory/production/group_vars/asa.yml
  roles:
    - asa_troubleshoot
    