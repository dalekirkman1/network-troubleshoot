---
- name: "12. Recent logs"
  cisco.asa.asa_command:
    commands:
      - show logging
  register: asa_logs

- name: "DEBUG | Recent logs (first 50 lines)"
  ansible.builtin.debug:
    msg: "{{ (asa_logs.stdout[0] | regex_replace('\\r', '')).split('\n')[0:50] }}"