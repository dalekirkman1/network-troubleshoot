# ============================================================
# Python Package Requirements
# Install with: pip install -r requirements.txt
#
# ansible-core    — Core Ansible engine (2.15+ required)
# paramiko        — SSH library for network_cli connection plugin
# textfsm         — TextFSM parsing engine for cli_parse tasks
# ntc-templates   — NTC community templates for Cisco IOS output
# ============================================================
ansible-core>=2.15.0
paramiko>=3.0.0
textfsm>=1.1.3
ntc-templates>=7.0.0
