---
# ============================================================
# LAB / TEST INVENTORY
# Safe to run against — will not affect production devices.
# ============================================================
all:
  children:
    ios_xe:
      hosts:
        lab-router1:
          ansible_host: 10.0.0.1
        lab-router2:
          ansible_host: 10.0.0.2
